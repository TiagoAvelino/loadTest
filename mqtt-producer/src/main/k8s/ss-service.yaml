apiVersion: v1
kind: Service
metadata:
  name: mqtt-producer
  namespace: kafka
spec:
  selector:
    app.kubernetes.io/name: mqtt-producer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
