quarkus.application.name=cons-kafka-prod-kafka
quarkus.otel.exporter.otlp.traces.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://dev-collector.openshift-tempo-operator.svc.cluster.local:4317}
quarkus.openshift.env.vars.otel-exporter-otlp-endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://dev-collector.openshift-tempo-operator.svc.cluster.local:4317}
quarkus.openshift.env.vars.otel-exporter-otlp-insecure=${OTEL_EXPORTER_OTLP_INSECURE:true}
quarkus.kubernetes-client.trust-certs=true
# quarkus.s2i.base-jvm-image=registry.access.redhat.com/ubi9/openjdk-21
# quarkus.openshift.deploy=true
mp.messaging.incoming."app.test".connector=smallrye-kafka
mp.messaging.incoming."app.test".topic=app.test
mp.messaging.incoming."app.test".key.deserializer=org.apache.kafka.common.serialization.StringDeserializer
mp.messaging.incoming."app.test".value.deserializer=org.acme.mqtt.MqttSendMessageDeserializerKafka
mp.messaging.incoming."app.test".group.id=cons-kafka-prod-kafka
# 
mp.messaging.outgoing."app.test.push".value.serializer=org.acme.mqtt.MqttSendMessageSerializer
mp.messaging.outgoing."app.test.push".value.deserializer=org.acme.mqtt.MqttSendMessageDeserializerKafka
mp.messaging.outgoing."app.test.push".topic=app.test.push
mp.messaging.outgoing."app.test.push".connector=smallrye-kafka
# Producer Performance Tuning
mp.messaging.outgoing."app.test.push".acks=1
mp.messaging.outgoing."app.test.push".compression.type=snappy
mp.messaging.outgoing."app.test.push".batch.size=16384
mp.messaging.outgoing."app.test.push".linger.ms=5

# Consumer Performance Tuning
mp.messaging.incoming."app.test".fetch.min.bytes=50000
mp.messaging.incoming."app.test".fetch.max.wait.ms=1000
mp.messaging.incoming."app.test".max.poll.records=500
#
# mp.messaging.outgoing.app-test-window.connector=smallrye-kafka
# mp.messaging.outgoing.app-test-window.topic=app.test.window
# mp.messaging.outgoing.app-test-window.value.serializer=org.acme.kafka.serdes.MqttSendMessageSerializer
# mp.messaging.outgoing.app-test-window.key.serializer=org.apache.kafka.common.serialization.StringSerializer
# Configs dev service.
# quarkus.kafka.devservices.port=9092
# quarkus.kafka.devservices.provider=kafka-native
# quarkus.kafka.devservices.topic-partitions."app.test"=3
# kafka.bootstrap.servers=localhost:9092
# quarkus.kafka.devservices.enabled=true
# quarkus.kafka.devservices.topic-partitions."app.test.push"=3
# quarkus.http.port=8084
# Reactive Messaging channel configuration (producer and consumer channels)
# Kafka cluster connection and security (SCRAM-SHA-512 over SASL_PLAINTEXT)
quarkus.openshift.env.vars.KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS:my-cluster-kafka-bootstrap-kafka.apps.tiago-cluster.sandbox1433.opentlc.com:443}
kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS}
kafka.security.protocol=SASL_SSL
kafka.sasl.mechanism=SCRAM-SHA-512
quarkus.openshift.env.mapping.KAFKA_USERNAME.from-secret=kafka-auth
quarkus.openshift.env.mapping.KAFKA_USERNAME.with-key=username
quarkus.openshift.env.mapping.KAFKA_PASSWORD.from-secret=kafka-auth
quarkus.openshift.env.mapping.KAFKA_PASSWORD.with-key=password
# Mount truststore secret into pod and bind env var
quarkus.openshift.mounts.truststore.path=/mnt/truststore
quarkus.openshift.secret-volumes.truststore.secret-name=kafka-client-ssl-secret
quarkus.openshift.env.mapping.TRUSTSTORE_PASSWORD.from-secret=kafka-client-ssl-secret
quarkus.openshift.env.mapping.TRUSTSTORE_PASSWORD.with-key=truststore-password
# kafka.sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required username=redhat-user password=redhat123;
kafka.sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required username="${KAFKA_USERNAME}" password="${KAFKA_PASSWORD}";
kafka.ssl.truststore.location=/mnt/truststore/truststore.jks
kafka.ssl.truststore.password=${TRUSTSTORE_PASSWORD}
# kafka.ssl.truststore.type=JKSmounts